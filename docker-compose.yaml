version: '3.1'

services:

  db:
    image: mongo:6.0
    container_name: chessless
    restart: always
    ports:
      - 27017:27017
    environment:
      MONGO_INITDB_ROOT_USERNAME: root
      MONGO_INITDB_ROOT_PASSWORD: my-password